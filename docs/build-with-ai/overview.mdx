---
sidebar_position: 0
sidebar_label: Overview
title: Build with AI
slug: /build-with-ai
read_time: 4
---

# Build an app with AI

You can describe an app in plain English and have an AI assistant write the whole
GDN app for you, both files. You don't have to know how to code. The key is
giving the AI the right context up front so it writes real GDN code instead of
guessing from another platform.

Building with AI is iterative: you reprompt for changes, and you
[edit the code yourself](/docs/build-with-ai/example-movies#dont-be-afraid-to-edit-it-yourself)
too, until it looks right. The [movies walkthrough](/docs/build-with-ai/example-movies)
shows that loop end to end.

<figure className="figure">
  <div className="panelRowStatic">
    <LedImage className="led led--glow" src="/img/examples/ai-steps.png" alt="A step-count app built from a prompt" width="260" />
    <LedImage className="led led--glow" src="/img/ai/ai-weather.png" alt="A weather panel built from a prompt" width="260" />
    <LedImage className="led led--glow" src="/img/examples/ai-score.png" alt="A scoreboard built from a prompt" width="260" />
  </div>
  <figcaption>Each of these was written by an AI from a one-line request, using the AI prompt.</figcaption>
</figure>

## What you'll need

An AI assistant. Any of these work, and the [next page](/docs/build-with-ai/choosing-a-tool)
helps you pick:

- **In your web browser**, with no install: [ChatGPT](https://chatgpt.com) or
  [Claude](https://claude.ai). Easiest way to start.
- **On your computer**, reading your actual project: [Claude Code](https://claude.com/claude-code)
  or the OpenAI Codex CLI. More powerful, a little more setup.

Either way the steps are the same:

1. **Give the AI the rules** (the [AI prompt](/docs/build-with-ai/prompt)).
2. **Describe your app** in a sentence or two.
3. **Drop the two files it writes into `apps/<your-app>/`** and open it in
   [Glance Dev Studio](/docs/studio).

## An app is just two files

Every GDN app is a folder with two files:

| File | What it is |
|---|---|
| `manifest.yaml` | The settings: the app's name, size, how often it refreshes, and the input form the user fills in. |
| `app.star` | The picture: [Starlark](/docs/reference/drawing-api) (Python-like) code that draws each screen. |

See [App fundamentals](/docs/overview/fundamentals) for the whole model. The AI writes
both files; you review and preview them.

## Step 1: give the AI the prompt

Open **[The AI prompt](/docs/build-with-ai/prompt)**, copy the box, and paste it into
your chat before you describe your app. It teaches the assistant the platform's rules
in one go, so it writes real GDN code instead of guessing.

<div className="cardGrid">
  <a className="card" href="/docs/build-with-ai/prompt"><h3>The AI prompt →</h3><p>The copy-paste block that primes your assistant. Paste it once at the start.</p></a>
</div>

## Step 2: describe your app

Right after the prompt, say what you want. A sentence works, but naming your
inputs and the panel width gets you a better result:

```text
Build me a GDN app that shows <what it shows>, with inputs for <the settings a
user should be able to change>. Target a 128x32 panel. Use helpers where they fit.
```

For example:

```text
Build me a GDN app that shows a habit streak: a big day-count with a small label
and a progress bar toward a goal. Inputs: the habit name and the goal number.
```

## Step 3: put it in your apps folder

The easiest way is to let [Glance Dev Studio](/docs/studio) make the folder for you:
click **Create New App**, and Studio creates the app folder plus starter `manifest.yaml`
and `app.star` files, then you paste the AI's code over them. Studio is the more
user-friendly option. See [Create a new app](/docs/studio/create-app).

If you'd rather do it by hand, the AI hands you two files, and getting them into
`apps/<your-app>/` is covered in
[Getting the AI's files into your project](/docs/build-with-ai/choosing-a-tool#getting-the-ais-files-into-your-project),
then open it in [Studio](/docs/studio) to see it live.

:::tip[AI code is a first draft, not the final word]

Always preview it. The mistakes assistants make most are lowercase text (fonts are
uppercase-only, so lowercase draws nothing) and reading an `http.get` response as
`resp.status_code` instead of `resp["status_code"]` — the response is a dict, so the
dotted form errors. `gdn check apps/<id>` and the live preview catch both in seconds.

:::

## Keep going

- **[Choosing your AI tool](/docs/build-with-ai/choosing-a-tool)**: browser vs.
  installed, with download and video links, and where the AI's two files go in `apps/`.
- **[Working with images](/docs/guides/working-with-images)**: importing, sizing, and
  placing a logo or icon, including dragging it into place in Studio.
- **[Examples & lessons](/docs/build-with-ai/examples)**: real prompts for popular
  app types, and the mistakes to avoid.
- **[Example: top 5 movies](/docs/build-with-ai/example-movies)**: a full, iterative
  build against a real API, from plain list to polished poster card.
